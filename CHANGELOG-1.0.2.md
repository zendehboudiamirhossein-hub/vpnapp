# وی پی ان من — نسخه 1.0.2

## اصلاح اتصال
- رفع خطای `xray.xudp.basekey` با واگذاری تولید BaseKey معتبر به Xray.
- نگه‌داشتن جزئیات فنی خطا برای عیب‌یابی، ولی نمایش پیام فارسی و خوانا به کاربر.
- هسته Xray روی `AndroidLibXrayLite v26.7.5` پین شده؛ این نسخه قبل از محدودیت جدید VLESS بدون TLS است و با سرورهای قدیمی اشتراک فعلی سازگاری بیشتری دارد.
- برای سرورهای جدید TLS/REALITY نیز ساختار کانفیگ فعلی حفظ شده است.

## رابط کاربری
- پس‌زمینه گرادیانی با نورهای متحرک.
- دکمه اتصال با هاله، Pulse و حلقه متحرک در حالت Connecting.
- وضعیت‌های اتصال با رنگ و کنتراست بهتر.
- کارت خطا کاملاً بازطراحی شده و دارای بخش «جزئیات فنی» است.
- کارت سرور و پنل انتخاب سرور تمیزتر شده‌اند.
- نسخه داخل صفحه درباره، به‌صورت خودکار از BuildConfig خوانده می‌شود.

## حجم APK
- فقط ABI `arm64-v8a` در خروجی تست قرار می‌گیرد.
- `material-icons-extended` حذف شد.
- دیتابیس‌های geoip/geosite که در کانفیگ فعلی استفاده نمی‌شوند از AAR حذف می‌شوند.
- Xray AAR روی نسخه سازگار `v26.7.5` پین شده تا Build قابل تکرار باشد.

> این خروجی برای گوشی‌های arm64 است که بخش عمده گوشی‌های اندرویدی امروزی را شامل می‌شود.
